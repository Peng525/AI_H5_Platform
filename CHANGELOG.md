# 更新日志

## 0.1.1 — 2026-09-22

- 生成结果页主按钮改为“保存为草稿”，成功提示可在工作台继续编辑，保存后留在当前页面。
- 保存期间显示“保存中…”并阻止重复请求；等待标题与画布保存，失败保留修改并支持重试。
- 标题自动保存与主动保存串行执行，避免旧请求覆盖新标题。
- 普通保存不再下载评估包或复制 Markdown；保留独立评估工具函数。
- 保存后的读取结果不再回填覆盖当前编辑器画布。

本版本不新增数据库草稿状态，不创建重复项目，不改变 PDF/PPTX 导出。
G1–G5 的其他工作区改动不属于本次提交。
