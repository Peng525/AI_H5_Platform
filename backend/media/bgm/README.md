# 背景音乐（推荐放这里）

`frontend` 构建会清空 `backend/static/`，因此 **请把 MP3 放在本目录**（`backend/media/bgm/`），后端仍通过 `/static/bgm/xxx.mp3` 提供访问。

标准文件名：`happier-sakura-girl.mp3`

安装脚本：`develop/scripts/install-bgm.ps1`（会写入本目录与 `static/bgm/` 两份）
