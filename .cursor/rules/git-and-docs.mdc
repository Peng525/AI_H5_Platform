---
description: 每次完成代码变更后 push；较大变更同步更新项目文档
alwaysApply: true
---

# Git 推送与文档同步

完成用户请求的实现后，默认执行以下流程（除非用户明确说不要 push）：

## Git

1. 检查 `git status` 与 `git diff`，确认变更范围。
2. 使用与仓库风格一致的 commit message（如 `feat:` / `fix:` / `docs:`）。
3. **推送到远程** `git push origin develop`（或当前工作分支）。
4. 若 push 失败，尝试诊断并重试；向用户说明结果。

## 项目文档（较大变更时必做）

「较大变更」包括但不限于：新功能、API 变更、页面结构/路由调整、环境变量变更、部署方式变更。

至少更新以下之一（按影响范围）：

- `README.md` — 功能概览、快速开始、配置说明
- `docs/需求清单.md` — 需求状态与变更记录
- `docs/部署说明.md` — 部署或使用步骤
- `docs/Stitch原型对照.md` — 原型与实现对照
- `.env.example` — 新增或变更的环境变量

较小修复（ typo、单行样式）可只 push，不必改文档。

## 安全

- 勿提交 `.env`、API Key、密钥文件。
- 勿 force push 到 main/master，除非用户明确要求。
