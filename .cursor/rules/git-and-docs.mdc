---
description: 每次 push 前检查清单；分支/部署/文档/安全规范
alwaysApply: true
---

# Git Push、发布与文档规范

完整说明见 [`docs/Push与发布规范.md`](docs/Push与发布规范.md)。每次开发完毕与 push 前**必须**执行下列检查。

## 开发完毕：Docker build（必做）

完成用户请求的实现后、push 或汇报完成前，**自行执行** Docker 构建并确认成功：

```bash
# 项目根目录（含 Dockerfile）
docker build -t ai-h5-platform:local .
```

- 构建**失败**：修复后重新 build，直至成功；勿 push、勿声称任务已完成
- 构建**成功**：再进入下方 Push 前检查
- 国内 Docker Hub 慢时可加：`docker compose -f docker-compose.yml -f docker-compose.cn.yml build`（与 CI 的 `docker build` 等价验证）

## Push 前检查（必做）

1. **安全**：`git status` / `git diff` 无 `.env`、API Key、`.pem`、`data/app.db`；新环境变量只进 `.env.example`（无真实密钥）。
2. **构建**：已通过上文 `docker build`（或 compose build）且 exit code 为 0
3. **文档**：较大变更同步 `README.md`、`docs/需求清单.md`、`docs/部署说明.md`、`.env.example`（见 Push 规范 §2.3）。
4. **分支**：日常 push `develop`；**禁止**未经用户确认 force push `main`。
5. **上线认知**：push `develop` **不会**更新 EC2；上云仅 merge `main` 后手动 **Deploy to EC2** workflow。

## 部署固定策略（勿擅自改动）

- `develop`：日常开发，CI 自动 build，**不**部署 EC2
- `main`：稳定 MVP，**仅** `workflow_dispatch` 手动 Deploy（`.github/workflows/deploy-ec2.yml`）
- 业务密钥 → EC2 `.env`；SSH 凭证 → GitHub Secrets（`EC2_HOST` / `EC2_USER` / `EC2_SSH_KEY`）
- 分享链接须在 **EC2 DNS、域名或公网 IP** 下发布（非 localhost）

## Git 操作

1. `git status` + `git diff` 确认范围
2. Commit：`feat:` / `fix:` / `docs:` / `chore:`；**仅用户明确要求时** commit
3. 默认 `git push origin develop`（或当前分支）；失败则诊断并重试
4. 不修改 git config；不 force push `main`（除非用户明确要求）

## 安全

- 勿提交 `.env`、密钥、收款码私钥路径中的敏感内容
- 勿将业务 JWT/LLM Key 写入 GitHub Secrets（仅 SSH 部署凭证可放 Secrets）
