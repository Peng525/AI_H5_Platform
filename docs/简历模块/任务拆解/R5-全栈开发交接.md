# R5 — 全栈开发 Agent 交接任务卡

> **状态**：已完成 · **优先级**：P0 · **基准 commit**：`40c273c feat: add resume module`  
> **承接角色**：全栈开发 Agent · **完成日期**：2026-07-03

---

## 1. 启动指令（复制到新对话）

```
请先读取 develop/docs/全局项目规范.md 与 develop/docs/全栈开发agent.md，以「全栈开发 Agent」角色承接 develop/docs/简历模块/任务拆解/R5-全栈开发交接.md 中的任务，严格按任务卡验收标准完成开发与交付。
```

---

## 2. 背景与已完成范围

简历模块 V1.0 主体已在 `develop` 分支 commit `40c273c` 落地，包含：

| 域 | 已完成 |
|----|--------|
| 后端 | `backend/app/api/resume.py`、`services/resume/*`、`services/ocr/paddle_provider.py`、5 个 `resume_*.yaml`、DB 模型 |
| 前端 | 生成页 Tab、三栏工作台、个人简历列表、`components/resume/*`、`useResumeDraft.js` |
| 文档 | `develop/docs/简历模块/` 全套 + 统一文档摘要 |
| 构建 | 曾通过 `docker build -t ai-h5-platform:local .`（develop 根目录） |

**Git 状态**：`develop` 比 `origin/develop` 超前 2 个 commit；工作区另有**幻灯片/编辑器**相关未提交改动，**禁止**混入本次简历任务 commit。

---

## 3. 必读文档（按序）

1. `develop/docs/全局项目规范.md`
2. `develop/docs/全栈开发agent.md`
3. `develop/docs/简历模块/README.md`
4. `develop/docs/简历模块/需求清单.md`（RS-01 ~ RS-54）
5. `develop/docs/简历模块/API规格.md`
6. `develop/docs/简历模块/功能开发说明书.md`
7. `develop/docs/简历模块/页面线框图.md`
8. `develop/docs/简历模块/开发映射.md`

---

## 4. 待办任务（按优先级）

### T1 — 列表缩略图 API（RS-05 缺口）【P0】

**问题**：`resume_service.list_item_payload` 返回 `thumbnail_url` 指向 `GET /api/v1/resume/{public_id}/thumbnail`，但 `resume.py` **未实现**该路由；`ResumeThumbCard` 目前为占位图标。

**要求**：

- 新增 `GET /api/v1/resume/{public_id}/thumbnail`（鉴权、404、过期校验）
- 在 `generate` / `optimize` / `PUT save` 成功后可生成缩略图（建议：结构化 JSON → 简单 HTML/CSS 渲染 → PNG，或 reportlab 简易预览图）
- 写入 `resume_profiles.thumbnail_path`，列表页可正常展示
- 同步更新 `API规格.md` 与 `功能开发说明书.md`

**验收**：`/dashboard/resumes` 卡片显示真实缩略图，非占位 icon。

---

### T2 — 端到端自测与缺陷修复【P0】

按 `需求清单.md` RS P0 项逐条验证，至少覆盖：

| 场景 | 预期 |
|------|------|
| 未登录访问简历 Tab / 工作台 | 跳转登录 |
| 上传 PDF ≤5MB | 解析成功进入工作台 |
| 上传 >5MB | 413 + Toast |
| 仅提示词生成 | generate 成功，扣 1 配额 |
| 聊天 optimize | 成功，扣 1 配额 |
| 保存 PUT | 不扣配额 |
| 导出 PDF/DOCX | 可下载，不扣配额 |
| 第 6 份简历 | 409 |
| 配额用尽 | 402 → `/upgrade` |
| 内容违规 | 422 + 提示 |

发现问题**仅修简历模块范围内**，不扩散到幻灯片编辑器未提交改动。

---

### T3 — Docker 构建验收【P0】

任务完成前在 `develop/` 根目录执行：

```bash
docker build -t ai-h5-platform:local .
```

构建失败须修复后重试，**不得**在未通过时声称完成。

---

### T4 — 配置与部署文档核对【P1】

- 确认 `.env.example` 中 `RESUME_*` 变量与 `config.py` 一致
- `部署说明.md` 补充：PaddleOCR 默认关闭；开启 `RESUME_PADDLEOCR_ENABLED=true` 时需额外安装 paddleocr（镜像体积大，V1 可选）

---

## 5. 明确不做（V1.0）

| ID | 说明 |
|----|------|
| RS-N01 | 微服务拆分 |
| RS-N02 | 中文 API 路径 |
| RS-52 | Chroma 向量检索（V1 用 LLM 诊断即可，**不实现**） |
| — | 合并工作区幻灯片/编辑器未提交改动 |
| — | 擅自修改 API 契约、三栏布局、配额规则 |

---

## 6. 交付格式（强制）

全栈开发 Agent 完成后必须输出四段：

1. **代码变更**：按路径列出修改/新增文件
2. **文档更新**：同步的 doc 路径与变更摘要
3. **Git 提交建议**：约定式 commit（如 `fix(resume): add thumbnail endpoint and list preview`）
4. **影响范围说明**：模块、接口、风险点、自测结果

---

## 7. 验收标准（产品架构 Agent 对照）

- [x] T1 缩略图 API + 列表展示通过
- [x] T2 RS P0 主流程自测通过（附简要测试说明）
- [x] T3 Docker build exit code 0
- [x] T4 配置文档无遗漏
- [x] 交付四段结构完整
- [x] 未混入非简历范围代码

---

## 8. 关键路径索引

| 类型 | 路径 |
|------|------|
| API | `backend/app/api/resume.py` |
| 业务 | `backend/app/services/resume/resume_service.py` |
| 列表页 | `frontend/src/views/ResumeList.vue` |
| 卡片 | `frontend/src/components/resume/ResumeThumbCard.vue` |
| 工作台 | `frontend/src/views/create/AiResumeWorkspace.vue` |
| 入口 Tab | `frontend/src/views/create/AiGenerateStart.vue` |
