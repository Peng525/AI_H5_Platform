# R7 — 简历编辑 / 优化分流 + 上传交互修复

> **状态**：已完成 · **优先级**：P0 · **需求基准**：[`需求清单-RS3-编辑优化分流.md`](../需求清单-RS3-编辑优化分流.md)  
> **承接角色**：全栈开发 Agent · **预估**：3.5 人日

---

## 1. 启动指令

```
请先读取 develop/docs/全局项目规范.md 与 develop/docs/全栈开发agent.md，以「全栈开发 Agent」角色承接 develop/docs/简历模块/任务拆解/R7-编辑优化分流.md，严格按 RS3 验收标准完成开发与文档同步。
```

---

## 2. 必读文档

1. [`需求清单-RS3-编辑优化分流.md`](../需求清单-RS3-编辑优化分流.md)
2. [`功能开发说明书-RS3-编辑优化分流.md`](../功能开发说明书-RS3-编辑优化分流.md)
3. [`需求清单-V2-可视化编辑.md`](../需求清单-V2-可视化编辑.md)（中间栏复用）
4. [`API规格.md`](../API规格.md)
5. [`组件复用说明.md`](../组件复用说明.md)

---

## 3. 任务拆解

### R7-1 文档（产品架构，开发同步检查）

- [x] `需求清单-RS3-编辑优化分流.md`
- [x] `功能开发说明书-RS3-编辑优化分流.md`
- [x] 更新 `用户故事.md`、`页面线框图.md`、`README.md`
- [ ] 开发完成后更新 `API规格.md`（`POST /resume` 增加 `template_id`）
- [ ] 开发完成后更新 `开发映射.md`

### R7-2 后端

| 文件 | 变更 |
|------|------|
| `backend/app/api/resume.py` | `CreateResumeBody.template_id` |
| `backend/app/services/resume/resume_service.py` | `create_profile` 空白 compile 分支 |
| `backend/tests/test_resume_create.py` 或扩展现有测试 | `template_id` 创建不扣配额、有 visual_document |

**验收**：`POST { "template_id": "classic-blue" }` 返回 `public_id`；`GET` 含空占位 structured + visual_document。

### R7-3 前端 — 生成页

| 文件 | 变更 |
|------|------|
| `views/create/AiGenerateStart.vue` | Tab 拆 `resume-edit` / `resume-optimize`；兼容 `?tab=resume` |
| `components/resume/ResumeOptimizeForm.vue` | **新建**：角标 + 提示词 + 生成 |
| `components/resume/ResumeAttachmentChip.vue` | **新建**：文件名 / 更换 / 移除 |
| `components/resume/ResumeFileUpload.vue` | 改为轻量触发器或并入 OptimizeForm |
| `composables/useResumeDraft.js` | 扩展 `tab`、附件字段 |
| `views/ResumeList.vue` | 链接 `?tab=resume-optimize`；可选「从模板新建」→ `resume-edit` |

**验收**：RS3-25 ~ RS3-30 上传交互；编辑 Tab 仅模板。

### R7-4 前端 — 工作台

| 文件 | 变更 |
|------|------|
| `views/create/AiResumeWorkspace.vue` | `mode` 布局；条件渲染 Advice |
| `components/resume/ResumeChatAside.vue` | **新建**：收起 / 展开 |
| `components/resume/ResumeThumbCard.vue` | 链至 `?mode=optimize` |

**验收**：`mode=edit` 两栏；`mode=optimize` 三栏；左栏可收起。

### R7-5 验收与构建

- [ ] 手工走 [`需求清单-RS3`](../需求清单-RS3-编辑优化分流.md) §四验收用例 1~9
- [ ] `docker build -t ai-h5-platform:local .`（develop 根目录）exit 0

**建议 commit**：

```
feat(resume): split edit/optimize tabs and fix upload attachment UX
```

---

## 4. 风险

| 风险 | 缓解 |
|------|------|
| Tab 增多导致生成页拥挤 | 简历两 Tab 并排；移动端可横向 scroll |
| 草稿 key 变更丢用户输入 | `?tab=resume` 映射 + 迁移读取旧 draft |
| 编辑创建误调 generate | 编辑 Tab `createFromTemplate` 禁止调用 generate API |
