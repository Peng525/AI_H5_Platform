# 废弃与待清理

集中记录已弃用或待删除的代码、依赖与文档漂移。

## 前端

| 项 | 位置 | 状态 | 弃用原因 | 替代方案 | 可删？ |
|----|------|------|----------|----------|--------|
| `AiGeneratePrompt.vue` | `views/create/` | 路由重定向 | 合并进单页生成 | `AiGenerateStart.vue` | 可删（确认无 deep link） |
| `ResultEditorToolbar.vue` | `components/create/` | 已删除 | result 页改用浮动栏 | `ElementQuickToolbar` + `TextFormatControls` | — |
| `TextFormatToolbar.vue` | `components/create/` | 已删除 | 合并进 `ElementQuickToolbar` | `TextFormatControls.vue` | — |
| `buildCoverWideSplit` | — | 已删除 | `colors.text` 侧栏盖住主题 | v5 `buildCover` | — |
| `vue3-draggable-resizable` | `package.json` | 未 import | 自研 pointer 拖拽 | `CanvasElement.vue` | 可从 dependencies 移除 |
| picsum 随机图 | 文档仍提及 | 代码已移除 | 无关随机配图 | `image_intent` 选择性配图 | 同步文档即可 |

## 后端

| 项 | 位置 | 状态 | 弃用原因 | 替代方案 | 可删？ |
|----|------|------|----------|----------|--------|
| `template_engine.py` | `services/` | 零引用 | 兼容 re-export | `prompt_template_service` 直接 import | **已删** |
| `create_paid_order` | `quota.py` | 未调用 | 旧订单路径 | `order_service.create_order` | **已删** |
| `list_claimed_orders` | `order_service.py` | 未调用 | admin 未接入 | 待 admin 需要时再实现 | **已删** |
| `new_share_slug` | `deck_generator.py` | 未调用 | 与 `deck_generation_service` 命名混淆 | `new_public_id` | **已删** |
| `pwd_context` 在 `api/auth.py` | — | 已迁移 | seed/admin 不应依赖 API 层 | `app/deps/security.py` | — |

## 文档漂移（需与代码一致）

| 文档 | 过期内容 | 正确状态 |
|------|----------|----------|
| `AI生成向导.md` | picsum fallback、compile v3 | 选择性配图、compile v5 |
| `功能开发说明书.md` | 同上 | 见 `幻灯片模板规范.md` |
| `需求清单.md` | picsum 相关条目 | 标记已完成/废弃 |

## 关联文件（删除前检查）

- 删除 `AiGeneratePrompt.vue`：检查 `router` 是否仅 redirect
- 删除 `vue3-draggable-resizable`：`npm run build` + grep package-lock
- 删除 `template_engine.py`：grep 全仓 `template_engine`

## 后续 refactor（不在 MVP 范围）

- `admin.py` / `projects.py` SQL 下沉到 service 层
- SQLite 内联 migration 换 Alembic
- 拆分 `useDeckEditor.js` 为多 composable
