# 功能开发说明书 — AI 生成向导与工作台

> 最后更新：2026-05-31 · 分支 `develop`  
> 面向开发：说明各页面 **按钮/控件的交互逻辑**、**本地状态**、**API 调用** 及 **发给 LLM 的最终内容**。

相关文档：[AI生成向导.md](./AI生成向导.md) · [需求清单.md](./需求清单.md)

---

## 一、范围与数据流

### 1.1 功能范围

| 模块 | 路由/组件 | 说明 |
|------|-----------|------|
| 生成入口 | `/create/generate` · `AiGenerateStart.vue` | 单页：类型 Tab、胶囊配置、提示词输入与示例 |
| 提示编辑器 | `/create/generate/review` · `AiGenerateReview.vue` | 微调并全量生成 |
| 图片生成 | `/create/generate/image` · `AiGenerateImage.vue` | 两栏生图：左编辑 / 右结果，点击裁切与复制 |
| 工作台壳 | `WorkbenchShell.vue` | 侧栏 + 顶栏入口 |

> `/create/generate/prompt` 已合并至生成入口，访问时 **重定向** 至 `/create/generate`。

### 1.2 草稿状态（sessionStorage）

键名：`ai_create_draft`（见 `useAiCreateDraft.js`）

| 字段 | 默认值 | 写入时机 |
|------|--------|----------|
| `type` | `deck` | 生成页选类型 |
| `pageCount` | `10` | 生成页 / 第 2 步底栏 |
| `background` | `classic_white` | 生成页 |
| `viewportMode` | `auto` | 生成页 |
| `language` | `简体中文` | 生成页 / 第 2 步 |
| `topic` | `''` | 生成页「继续」 |
| `textDensity` | `精炼` | 第 2 步 |
| `audience` | `''` | 第 2 步 |
| `tone` | `专业、清晰、具说服力` | 第 2 步 |
| `extraContent` | `''` | 生成页 / 第 2 步 |
| `extraInstructions` | `''` | 第 2 步 |
| `contentMode` | `free` | 第 2 步：`free` / `per_page` |
| `cardSplitMode` | `null` | 第 2 步：`auto` / `manual` |
| `pageContents` | `[]` | 第 2 步逐张卡片各页文本 |

全量生成成功后调用 `clearDraft()` 清空草稿。

---

## 二、生成入口（`/create/generate`）

**组件：** `frontend/src/views/create/AiGenerateStart.vue`  
**布局壳：** `AiCreateLayout` — 柔和纵向渐变背景（`from-sky-100/50 via-white to-sky-50/80`）；顶栏 `AiCreateHeader` 半透明毛玻璃（`bg-white/80 backdrop-blur`）。

### 2.1 标题区

居中展示「生成」+「您今天想创建什么？」。

### 2.2 类型 Tab（横向按钮）

| Tab | 点击逻辑 | 后续影响 |
|-----|----------|----------|
| **演示文稿** | `type = 'deck'`；选中态 `border-primary bg-primary/5 text-primary` | 显示胶囊配置、输入框与示例提示 |
| **生成图片** | `type = 'image'` | 显示 **图片比例 / 颜色 / 风格** 三胶囊 + 输入框（宽度与 deck 一致 `max-w-3xl`）；空态分割线下方 **3 张模板卡片**；「编辑提示词」跳转图片页 |

进入页面 **默认选中演示文稿**（不恢复草稿中的 `type`）。Tab 居中排列、适中尺寸（`min-w-[7rem]`）。

### 2.3 胶囊配置（仅 `type === 'deck'`）

白底、`rounded-full` 下拉（页数 / 背景 / 尺寸 / 语言），**左对齐**（`justify-start`）：

| 控件 | 选项 | 草稿字段 | 后端映射 |
|------|------|----------|----------|
| 页数 | 1–10 张卡片 | `pageCount` | `page_count` |
| 背景 | 经典白粉 / 浅灰 | `background` | `background_preset` → `#fafafa` / `#f0f2f5` |
| 尺寸 | 默认动态 / 传统网页 / 移动端 | `viewportMode` | `auto`→`mobile-375`；`web`→`web-1280`；`mobile`→`mobile-375` |
| 语言 | 简体中文 / English | `language` | `language` |

### 2.3.1 胶囊配置（仅 `type === 'image'`）

白底、`rounded-full` 下拉，**左对齐**，位于输入框上方；选项见 `imageGenerateOptions.js`：

| 控件 | 选项 | 草稿字段 | 说明 |
|------|------|----------|------|
| 图片比例 | 9:16 移动端 / 16:9 网页 | `viewportMode`（`mobile` / `web`） | 映射 `mobile-375` / `web-1280` |
| 颜色 | 经典白粉 / 浅灰 / 深色 / 多彩 | `imageColor` | 生图页拼入提示词（色调） |
| 风格 | 扁平插画 / 3D质感 / 水彩插画 / 商务专业 / 摄影写实 | `imageStyle` | 选模板时同步 `suggestedStyle`；生图页拼入提示词 |

### 2.4 提示词输入与示例（Gamma 两态）

**空态**（`!topic.trim()`）

| 元素 | 行为 |
|------|------|
| 输入框 | 单行紧凑（~44px），随输入 `resizeTopicInput` 增高，最高 ~192px 后框内滚动 |
| 示例（仅 `deck`） | 显示分割线 + 示例卡片 +「换一组」 |
| 生图模板（仅 `image`） | 输入框下方分割线 + **3 张横向模板卡片**（与输入框同宽 `max-w-3xl`；标题 + 主体/环境/光线/风格全文）；数据源 `useImagePromptTemplates` |
| 主按钮 | **不显示** |

**有输入**（`topic.trim()` 非空）

| 元素 | 行为 |
|------|------|
| 输入框 | 继续自动增高至上限 |
| 示例 / 模板 | **隐藏** |
| **编辑提示词** | 输入框下方**居中**；图标 `auto_awesome`；点击 `goNext()` |

**生图模板卡片**：点击后将 `formatImagePromptTemplate(tpl)` 写入 `topic`，选中态高亮（`check_circle`）；清空输入后模板区重新显示。

共用：粘贴后滚顶；框外右下角字数；`deck` / `image` 均使用同一输入框。

### 2.5 「编辑提示词」跳转

| 类型 | 启用条件 | 逻辑 | 目标 |
|------|----------|------|------|
| `deck` | `topic.trim()` 非空 | `saveDraft({ type, pageCount, background, viewportMode, language, topic })` | `/create/generate/review` |
| `image` | `topic.trim()` 非空 | `saveDraft({ type, viewportMode, imageColor, imageStyle, topic })` | `/create/generate/image`（预填 `topic`、尺寸与风格/色调元数据） |

**不调用 API**，仅写草稿并跳转。

### 2.6 入口顶栏与配额

- 顶栏使用 `AiCreateHeader`（`variant="entry"`）：左上「主页」「我的工作台」、居中「欢迎来到 AI H5 平台」、右上 `UserMenu`
- `UserMenu`：桌面端 **用户名下方** 以 `text-xs` 展示 **`tier · 配额`**（如 `专业版 · 配额 97/100`）；窄屏在下拉菜单首行展示
- 工作台二级侧栏用户名下方同样展示配额（`text-xs`）；Dashboard / 模板库主内容区不重复展示

---

## 三、提示词页（已合并）

原 `/create/generate/prompt` · `AiGeneratePrompt.vue` 已 **合并至 §2 生成入口**。路由保留 **重定向** 至 `/create/generate` 以兼容旧链接。

---

## 四、提示编辑器（`/create/generate/review`）

**组件：** `frontend/src/views/create/AiGenerateReview.vue`

**前置：** 无 `topic` 时 `router.replace('/create/generate')`。

### 4.1 顶栏

| 按钮 | 逻辑 |
|------|------|
| **上一步** | `/create/generate` |

### 4.2 移动端 Tab（`< lg`）

| Tab | 显示区域 |
|-----|----------|
| 设置 | 左栏：文本量、受众、语气、语言 |
| 内容 | 中栏：扩写内容 |
| 说明 | 右栏：附加说明 |

桌面端三栏同时展示。

### 4.3 文本量四按钮（核心）

| 按钮 | 点击逻辑 | 是否立即请求 AI |
|------|----------|-----------------|
| **简约** | `textDensity = '简约'` | 否 |
| **精炼** | `textDensity = '精炼'`（默认） | 否 |
| **详细** | `textDensity = '详细'` | 否 |
| **繁琐** | `textDensity = '繁琐'` | 否 |

选中态：主色边框 + 浅蓝底。

**仅在点击「生成」时** 随请求体发送 `text_density`。后端映射（`deck_generation_service.py`）：

| 值 | 写入 LLM `style` 字段的文案 |
|----|------------------------------|
| 简约 | 简洁明了，每页要点极少 |
| 精炼 | 精炼专业，重点突出 |
| 详细 | 详细展开，信息充实 |
| 繁琐 | 详尽全面，覆盖细节 |

同时在 `全量生成.yaml` 约束行出现：`文本密度为 {{ text_density }}`（原始中文值）。

### 4.4 内容栏双模式

内容栏头部 Segmented 切换：**自由形式**（默认） / **逐张卡片**。

| 模式 | UI | 生成行为 |
|------|-----|----------|
| **自由形式** | 单个 textarea，`extraContent` | `content_mode: free`；`topic` + `extra_content` 合并；LLM 按页数与风格自行分页 |
| **逐张卡片** | N 个带「页面 i」badge 的 textarea | `content_mode: per_page`；`page_contents[]` 与 `page_count` 等长；LLM 严格 1 页对应 1 段 |

**切换到逐张卡片**（首次）：弹出 `CardSplitModeDialog`：

| 按钮 | 逻辑 |
|------|------|
| **自动分页** | 调用 `splitContentIntoPages(extraContent, pageCount)`：按 `# 标题`、中文序号、`1.` 数字序号、双换行段落切分，再归一化到 N 段 |
| **自行分页** | `pageContents = N 个空字符串` |
| **取消** | 不切模式 |

**自动分页规则**（`frontend/src/utils/splitContentIntoPages.js`）：

1. 段数 > N：合并相邻最短段  
2. 段数 < N：拆分最长段直至 N 段  
3. 空文本：N 个空白框  

**切回自由形式**：`pageContents.filter(Boolean).join('\n\n')` 写回 `extraContent`，清空 `cardSplitMode`。

**页数 ± 与逐张卡片**：增减 `pageCount` 时 `syncPageContents` 对齐数组；减少且末尾页有内容时 ConfirmDialog 确认丢弃。

### 4.5 其他设置控件

| 控件 | 草稿/API 字段 | 发给 LLM 的方式 |
|------|---------------|-----------------|
| **写给…** | `audience` | 模板变量 `受众：{{ audience }}` |
| **语气** | `tone` | 拼入 `style`：`语气：{tone}` |
| **语言** | `language` | 约束行 + `aiLanguage` 写入项目 settings |
| **内容（自由形式）** | `extra_content` | 与 `topic` 合并为 `_build_topic()` |
| **内容（逐张卡片）** | `page_contents[]` | 模板逐页列出；`topic` 作整体主题参考 |
| **附加说明** | `extra_instructions` | 非「无」时追加：`附加说明：{{ extra_instructions }}` |

### 4.6 底栏

| 按钮 | 逻辑 |
|------|------|
| **−** | `pageCount = max(1, pageCount - 1)` |
| **+** | `pageCount = min(30, pageCount + 1)` |
| **生成** | 见 §4.7 |

页数显示：`{pageCount} 张卡片`（前端可调到 30，生成页下拉仍为 1–10）。

### 4.7 「生成」按钮 — 完整链路

1. `saveDraft(...)` 保存当前编辑器状态  
2. `POST /api/v1/项目/ai-生成`，请求体：

```json
{
  "topic": "<生成页 topic>",
  "page_count": 10,
  "audience": "<写给…>",
  "tone": "<语气>",
  "text_density": "简约|精炼|详细|繁琐",
  "language": "简体中文|English",
  "viewport_mode": "auto|web|mobile",
  "background_preset": "classic_white|light_gray",
  "extra_content": "<内容区>",
  "extra_instructions": "<附加说明>",
  "content_mode": "free|per_page",
  "page_contents": ["第1页…", "第2页…"]
}
```

**逐张卡片模式校验**：至少 1 页非空；`page_contents.length === page_count`。

3. 后端消耗 1 次 AI 配额 → 渲染 `全量生成.yaml` → LLM → 解析 JSON → 创建项目与 slides  
4. 前端 `applyProjectSettingsLocal` + `clearDraft()` → `/editor/:id`

**LLM 系统提示结构（摘要）：**

```
约束：页数为 N；语言为 …；文本密度为 …；…
[free] 分页模式：自由形式。请根据页数与风格自行将主题内容合理分页。
      主题：{topic + extra_content 合并}
[per_page] 分页模式：逐张卡片。必须恰好 N 页，第 i 页严格对应「第 i 页」文本。
           整体主题参考：{topic}
           --- 第 1 页 --- …
受众：…
风格：{密度描述；语气：…}
```

失败：底栏显示 `error`，`generating` 复位，可重试。

---

## 五、图片路径（`/create/generate/image`）

**组件：** `frontend/src/views/create/AiGenerateImage.vue`

**布局：** 桌面端 `lg:grid-cols-2` 左右等宽两栏，窄屏上下堆叠；左右栏始终同时可见（无 form/result 整页切换）。

| 区域 | 控件 | 逻辑 |
|------|------|------|
| 左栏 · 提示词编辑 | 提示词 textarea | 编辑 `prompt`；自生成页预填 `draft.topic` |
| 左栏 | 尺寸 | `mobile` / `web` → `viewport_preset_id` |
| 左栏 | **生成图片** | `POST /api/v1/生图/独立`；按钮 `progress_activity` 旋转加载 |
| 右栏 · 生成结果 | 提示词摘要 | 成功后展示本次 `resultPrompt` |
| 右栏 | 图片预览框 | 空态占位 / 生成中 spinner / 成功后加载图片 |
| 右栏 | 点击图片 | 打开 `ImageCropModal` 裁切 |
| 右栏 | **复制** | 剪贴板写入图片（fallback 链接） |
| 顶栏 | **返回首页** | `/create/generate` |

**独立生图链路：**

1. `generateStandaloneImage({ prompt, viewport_preset_id, fit_mode, tier })`
2. 扣 AI 配额，`GenerationLog.project_id = null`
3. 页面展示结果预览，**不进入编辑器**

---

## 六、入口页顶栏（`/create/generate`）

**组件：** `AiCreateHeader.vue`（`variant="entry"`）

| 区域 | 内容 | 路由 |
|------|------|------|
| 左上 | 主页（高亮）、我的工作台 | `/create/generate`、`/dashboard` |
| 居中 | 欢迎来到 AI H5 平台 | — |
| 右上 | 头像 + 名称下拉 | 使用帮助、退出登录 |

**配额：** 生成向导顶栏 `UserMenu` 在 **用户名下方** 以 `text-xs` 展示（如 `专业版 · 配额 97/100`）；工作台侧栏用户名下方同样展示；Dashboard / 模板库主内容区不重复。

---

## 七、工作台（`WorkbenchShell.vue`）

三栏布局：**深色图标栏**（跨页切换）→ **二级工作区侧栏**（上下文）→ **主内容区**（页面自有工具栏）。

### 7.1 图标栏（`w-16`，桌面端）

| 图标 | 路由 | 说明 |
|------|------|------|
| 主页 | `/create/generate` | AI 生成入口（非工作台） |
| 工作台 | `/dashboard` | 项目列表 |
| 模板库 | `/templates` | H5 模板探索 |
| 底部头像 | 下拉菜单 | 使用帮助、升级会员、退出登录 |

图标按钮 `w-12`，Material 图标 `26px`。**不与二级侧栏重复**路由文字导航。

### 7.2 二级工作区侧栏

| 项 | 行为 | 显示条件 |
|----|------|----------|
| 工作区标题 | 用户名（`text-base`）+ **配额**（`text-xs`） | 始终 |
| 升级 CTA | → `/upgrade` | 非 Pro |
| **全部项目** | 蓝底白字高亮；等同 Dashboard | `/dashboard` |
| **探索模板** | 蓝底白字高亮；等同模板库 | `/templates` |
| 管理控制台 | → `/admin` | 管理员 |
| 移动端底部 | 帮助、升级、退出 | `sm:hidden` |

### 7.3 全局顶栏（移动端）

| 控件 | 逻辑 |
|------|------|
| **菜单**（仅 `sm:hidden`） | 打开二级侧栏抽屉 |

桌面端无全局顶栏；主内容区 **白底**；**配额不在 Dashboard / 模板库主内容工具栏重复显示**。

### 7.4 Dashboard 内容区（`/dashboard`）

**组件：** `Dashboard.vue` · `ProjectCoverThumb.vue` · `usePptImport.js`

| 区域 | 控件 | 逻辑 |
|------|------|------|
| 标题 | **项目** | 页面主标题 |
| 标题下方 | **+ 新建演示**（主按钮）、**导入 PPT**（左）；**批量删除**（右） | 同行左右分列 |
| 项目卡片 | 缩略图 + 标题 + 相对时间 | `...` 菜单：编辑 / 预览 / 删除 |

主内容区背景为 **白底**（`WorkbenchShell` 右侧列）。已移除 Tab 筛选与搜索框。

**按钮样式：** **新建演示** 为蓝底白字主按钮（`bg-primary text-on-primary`，与侧栏「全部项目」一致）；**导入 PPT** 及批量操作中的次要按钮为白底黑字 + 灰边框；批量删除与「删除选中」为红底白字。

**字号：** 页面主标题「项目」与卡片标题保持原有强调级别；其余正文、按钮、元数据（相对时间）、空状态文案、批量选择提示等统一为 `text-sm`。

导入限制：仅 `.pptx`；失败显示内容区红色提示。

### 7.5 模板库内容区（`/templates`）

**组件：** `Templates.vue` · `TemplateCoverThumb.vue`

| 区域 | 控件 | 逻辑 |
|------|------|------|
| 侧栏 | **探索模板** | 蓝底白字高亮（与 Dashboard「全部项目」对称） |
| 标题 | **模板库** + 副标题「探索模板，快速创建 H5 演示」 | 图标 `22px`；`space-y-6` 区块间距 |
| 搜索 | `max-w-md` 短条（内嵌 `search` 图标）+ **胶囊**搜索按钮 | `rounded-full` 主按钮 |
| 筛选 | **单行** chip：全部 / 移动端 / 网页版 + 各分类 | 未选中灰底；选中 `bg-blue-100 text-blue-800`；终端与分类互斥 |
| 模板卡片 | 与项目卡片同网格 | `xl:grid-cols-4`；缩略图（终端+页数角标）→ 标题 + `分类 · N 页`；**点击整卡** → `ConfirmDialog` 确认创建 |
| 空状态 | 虚线框 + 新建演示链接 | 与 Dashboard 空项目态风格一致 |

主内容区背景为 **白底**（`WorkbenchShell` 右侧列）。加载态使用 `PageLoading`。

---

## 八、共享布局（`AiCreateLayout.vue`）

| 元素 | 逻辑 |
|------|------|
| 返回工作台 / 上一步 | 由父组件 `@back` 或默认链 `/dashboard` |
| 用户菜单 | entry / step 顶栏右上 `UserMenu`（含 `text-xs` 配额） |
| `fillHeight` | 子页面（提示词页）全高 flex，主区不滚动 |
| `#footer` 插槽 | 固定底栏（如「继续」） |

---

## 九、后端 API 速查

| 端点 | 触发按钮 | 配额 |
|------|----------|------|
| `POST /api/v1/项目/ai-生成` | 提示编辑器「生成」 | 是 |
| `POST /api/v1/项目/导入-pptx` | Dashboard「导入 PPT」 | 否 |
| `POST /api/v1/生图/独立` | 生成图片页「生成图片」 | 是 |
| `POST /api/v1/项目` | 图片页创建空项目 | 否 |

---

## 十、源码索引

| 文件 | 职责 |
|------|------|
| `frontend/src/views/create/AiGenerateStart.vue` | 生成入口（单页 Tab + 配置 + 提示词） |
| `frontend/src/views/create/AiGeneratePrompt.vue` | 已废弃（路由重定向至 generate） |
| `frontend/src/views/create/AiGenerateReview.vue` | 提示编辑器 |
| `frontend/src/views/create/AiGenerateImage.vue` | 图片两栏（编辑 / 结果 / 裁切 / 复制） |
| `frontend/src/composables/useAiCreateDraft.js` | 草稿与示例词库 |
| `frontend/src/utils/splitContentIntoPages.js` | 自动分页规则 |
| `frontend/src/components/create/CardSplitModeDialog.vue` | 分页方式弹窗 |
| `frontend/src/components/create/AiCreateHeader.vue` | 入口/步骤顶栏 |
| `frontend/src/components/create/UserMenu.vue` | 用户下拉菜单 |
| `frontend/src/components/create/AiCreateLayout.vue` | 向导壳 |
| `frontend/src/components/WorkbenchShell.vue` | 工作台壳 |
| `frontend/src/components/WorkbenchAvatarMenu.vue` | 图标栏用户菜单 |
| `frontend/src/components/ProjectCoverThumb.vue` | 项目卡片缩略图 |
| `frontend/src/composables/usePptImport.js` | PPT 导入 |
| `backend/app/services/deck_generation_service.py` | 全量生成与 text_density 映射 |
| `backend/templates/全量生成.yaml` | LLM 提示词模板 |
| `backend/app/schemas.py` | `AiDeckGenerateRequest` |

---

## 十一、变更记录

| 日期 | 说明 |
|------|------|
| 2026-05-31 | 生成页 Gamma 两态：空态单行输入 + deck 示例 / image 三模板卡片；有输入居中「编辑提示词」；自适应增高输入框 |
| 2026-05-31 | image Tab：三胶囊（比例/颜色/风格）+ 与 deck 同宽输入框 + 分割线下方三列模板；draft 增 `imageColor` / `imageStyle` |
| 2026-05-31 | 图片页返回首页、两栏等高；去掉面包屑 |
| 2026-05-31 | 图片页两栏布局：左「提示词编辑」+ 旋转加载生成；右「生成结果」+ 点击裁切 + 复制 |
| 2026-05-31 | 生成页 UI 微调：Tab/胶囊尺寸、输入框边框、图片模式共用输入、默认 deck |
| 2026-05-31 | 生成页 Gamma 单页：合并 prompt 至 generate、渐变背景、横向 Tab、胶囊配置、示例卡片；UserMenu 配额叠于用户名下 |
| 2026-06-01 | 图标栏三入口（主页/工作台/模板）、Dashboard 布局微调、移除导入尺寸下拉 |
| 2026-06-01 | 生成页顶栏去重、Dashboard 筛选布局、配额仅侧栏用户名下方 |
| 2026-06-01 | 工作台 Gamma 布局：图标栏/二级侧栏去重、Dashboard 内容顶栏与卡片缩略图 |
| 2026-06-01 | 移除「创建空白项目」入口；`/create/blank` 重定向至生成页 |
| 2026-06-01 | 入口页欢迎顶栏、独立生图 API、图片裁切/复制/下载 |
| 2026-06-01 | 初版：AI 向导四步 + 工作台全部按钮逻辑与 LLM 映射 |
